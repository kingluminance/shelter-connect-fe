// Player walk-cycle sheet: 8 columns x 6 rows, 24x24px per frame (192x144 total).
// Rows look visually interchangeable (single forward-facing pose) — row/frame meaning
// beyond "a walk cycle" is unconfirmed with the artist, see docs/game-architecture.md.
export const playerWalkSheet = require('./player-walk.png');
export const PLAYER_FRAME_SIZE = 24;
export const PLAYER_SHEET_COLS = 8;
export const PLAYER_SHEET_ROWS = 6;
export const PLAYER_WALK_ROW = 0;
export const PLAYER_WALK_FRAME_COUNT = 8;
